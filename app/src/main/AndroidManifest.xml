<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cityparcel">

    <!-- authority -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <!-- serviceHome package -->
        <activity android:name=".serviceHome.ServiceMainActivity"/>
        <!-- login package-->
        <activity android:name=".login.LoginWindowActivity" android:windowSoftInputMode="adjustResize|stateHidden"/>
        <!-- register package-->
        <activity android:name=".register.DaumWebViewActivity"/>
        <activity android:name=".register.RegisterTermsActivity" />
        <activity android:name=".register.SmsValidityActivity" android:windowSoftInputMode="adjustResize|stateHidden"/>
        <activity android:name=".register.RegisterAddressFormActivity" android:windowSoftInputMode="adjustResize|stateHidden"/>
        <activity android:name=".register.RegisterFormActivity" android:windowSoftInputMode="adjustResize|stateHidden"/>
        <!-- root package-->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>